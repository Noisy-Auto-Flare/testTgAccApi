# 🔐 Гайд по авторизации и ошибкам

## Проблема: RPCError 406: UPDATE_APP_TO_LOGIN

Если вы видите эту ошибку:
```
RPCError 406: UPDATE_APP_TO_LOGIN (caused by SendCodeRequest)
```

**Это означает:** Telegram требует дополнительной проверки при первой авторизации через кастомный API клиент.

---

## ✅ Решение: Используйте готовый .session файл

### Вариант 1: Авторизоваться через официальное приложение (рекомендуется)

**Шаг 1**: Авторизуйтесь в **официальном приложении Telegram**
- Скачайте Telegram Desktop или используйте web.telegram.org
- Авторизуйтесь со своим номером телефона
- Получите SMS код и введите его

**Шаг 2**: Экспортируйте .session файл
```bash
# Обычно сессия находится здесь:
# Linux: ~/.local/share/TelegramDesktop/tdata/D<...>/
# macOS: ~/Library/Application Support/Telegram Desktop/tdata/
# Windows: %APPDATA%\Telegram Desktop\tdata\

# Скопируйте файл вашей сессии в проект
cp /path/to/your/session.session ./sessions/myaccount.session
```

**Шаг 3**: Используйте сессию в config.json
```json
{
  "telegram": {
    "api_id": 123456,
    "api_hash": "your_api_hash"
  },
  "accounts": [
    {
      "name": "myaccount",
      "session_file": "./sessions/myaccount.session",
      "phone": null,
      "llm": {
        "enabled": false
      },
      "media_forward": {
        "enabled": false
      }
    }
  ]
}
```

**Шаг 4**: Запустите бота
```bash
python3 main.py
```

---

### Вариант 2: Подождите и повторите

Иногда Telegram просто требует времени:
1. **Закройте приложение**
2. **Подождите 10-30 минут**
3. **Попробуйте снова**

---

### Вариант 3: Используйте VPN (если вы в РФ)

Telegram иногда затрудняет авторизацию в России. Используйте:
```json
{
  "telegram": {
    "api_id": 123456,
    "api_hash": "your_api_hash",
    "proxy": {
      "proxy_type": "socks5",
      "addr": "127.0.0.1",
      "port": 1080
    }
  }
}
```

---

## 📱 Как получить .session файл

### Способ 1: Через Python (рекомендуется)

```python
from telethon import TelegramClient
import asyncio

async def export_session():
    # Ваши API credentials
    api_id = 123456
    api_hash = "your_api_hash"
    phone = "+79001234567"
    
    # Создаём клиент
    async with TelegramClient('myaccount', api_id, api_hash) as client:
        # Авторизуемся
        await client.start(phone=phone)
        print("✓ Авторизация успешна")
        print("Файл сохранён как: myaccount.session")

asyncio.run(export_session())
```

### Способ 2: Скопировать из Telegram Desktop

**На macOS:**
```bash
cp ~/Library/Application\ Support/Telegram\ Desktop/tdata/D*/myaccount.session ./sessions/
```

**На Linux:**
```bash
cp ~/.local/share/TelegramDesktop/tdata/D*/myaccount.session ./sessions/
```

**На Windows:**
```bash
copy "%APPDATA%\Telegram Desktop\tdata\D*\myaccount.session" ".\sessions\"
```

---

## 🔄 Автоматическая авторизация

Код **автоматически использует** device параметры для идентификации:

```python
TelegramClient(
    session_name,
    api_id,
    api_hash,
    device_model='Desktop',      # ← Помогает избежать ошибки
    system_version='Linux',       # ← Идентификация системы
    app_version='1.0'             # ← Версия приложения
)
```

---

## ❓ FAQ

### Q: Может ли мой аккаунт быть заблокирован?
**A:** Нет. Telegram не блокирует за использование API. Это легальный способ взаимодействия.

### Q: Почему UPDATE_APP_TO_LOGIN если я уже авторизован?
**A:** Telegram требует дополнительной проверки при первом подключении нового клиента.

### Q: Нужно ли вводить SMS код каждый раз?
**A:** Нет, если вы используете .session файл. Код нужен только при первой авторизации.

### Q: Сессия может истечь?
**A:** Да, если не использовать аккаунт 6+ месяцев. Пересохраните сессию.

### Q: Могу ли я использовать один .session файл для нескольких ботов?
**A:** Нет, каждый процесс должен иметь свой .session файл.

---

## 🛠️ Диагностика

Если ничего не работает, используйте диагностический скрипт:

```bash
python3 setup.py
```

Скрипт проверит:
- ✓ Наличие config.json
- ✓ Валидность API credentials
- ✓ Доступность API Telegram
- ✓ Права на доступ к файлам

---

## 📚 Дополнительные ресурсы

- **CONFIG_USAGE_GUIDE.md** - Примеры конфигураций
- **README.md** - Основная документация
- **main.py** - Исходный код инициализации

