# 🚀 Быстрая справка / Quick Start Guide

## 📋 Минимум 5 минут до запуска

### 1️⃣ Установка (2 минуты)

```bash
# Установить зависимости
pip install -r requirements.txt

# Создать конфиг
cp config.example.json config.json
```

### 2️⃣ Получить API credentials (2 минуты)

1. Откройте https://my.telegram.org/
2. Войдите по номеру телефона
3. Перейдите в "API development tools"
4. Скопируйте **api_id** и **api_hash**

### 3️⃣ Отредактировать config.json (1 минута)

```bash
nano config.json
```

Заполните:
```json
{
  "telegram": {
    "api_id": YOUR_API_ID,
    "api_hash": "YOUR_API_HASH"
  },
  "accounts": [
    {
      "name": "myaccount",
      "phone": "+79001234567"
    }
  ]
}
```

### 4️⃣ Запустить

```bash
python3 main.py
```

При первом запуске:
- Введите номер телефона
- Введите код из Telegram
- Готово!

---

## 🎯 Использование функций

### 🔑 Функция 1: .session файлы

**Для чего**: Не вводить код каждый раз, использовать готовый файл сессии

**Как**:
```bash
# Способ 1: Через setup.py
python3 setup.py
# Выбрать пункт 2, следовать инструкциям

# Способ 2: Вручную
# Скопировать existing_session.session в sessions/
cp ~/existing.session sessions/myaccount.session

# В config.json:
{
  "session_file": "./sessions/myaccount.session",
  "phone": null
}
```

### 🤖 Функция 2: LLM (авто-ответы)

**Для чего**: Автоматически отвечать на сообщения через AI

**Установка text-generation-webui**:
```bash
git clone https://github.com/oobabooga/text-generation-webui.git
cd text-generation-webui
bash start_linux.sh  # или start_macos.sh / start_windows.bat
```

**В config.json**:
```json
{
  "llm": {
    "enabled": true,
    "api_url": "http://127.0.0.1:5000/api/v1/chat/completions",
    "system_prompt": "Ты дружелюбный ассистент. Помогай пользователям.",
    "allowed_chat_ids": [123456789]
  }
}
```

**Как узнать ID чата**:
```bash
python3 setup.py
# Выбрать пункт 3
```

### 📹 Функция 3: Media Forwarder (пересылка медиа)

**Для чего**: Автоматически пересылать фото/видео в архивный канал

**В config.json**:
```json
{
  "media_forward": {
    "enabled": true,
    "source_chat_ids": [123456789],
    "target_channel_id": -100987654321
  }
}
```

---

## 📚 Основные файлы

| Файл | Описание |
|------|---------|
| `main.py` | Запуск всех функций |
| `setup.py` | Настройка и получение IDs |
| `examples.py` | Примеры кода |
| `config.json` | Ваша конфигурация |
| `README.md` | Полная документация |
| `INSTALLATION.md` | Подробная инструкция |

---

## 🔧 Основные команды

```bash
# Запуск основного скрипта
python3 main.py

# Интерактивная настройка
python3 setup.py

# Примеры кода
python3 examples.py

# Классический скрипт (проверка аккаунта)
python3 check_telegram_account.py
```

---

## ⚠️ Частые ошибки

| Ошибка | Решение |
|--------|---------|
| `config.json не найден` | `cp config.example.json config.json` |
| `ModuleNotFoundError` | `pip install -r requirements.txt` |
| `API не отвечает` | Проверьте api_id и api_hash |
| `LLM не работает` | Запустите text-generation-webui: `bash start_linux.sh` |
| `Сессия недействительна` | Удалите `.session` файл и авторизуйтесь заново |

---

## 💡 Советы

1. **Для отладки**: Проверьте файл `chat_ids.json` после первого запуска `setup.py`
2. **Безопасность**: Никогда не коммитьте `config.json` и `.session` файлы
3. **Производительность**: Используйте `allowed_chat_ids` чтобы обрабатывать только нужные чаты
4. **Модели LLM**: Используйте Mistral 7B для оптимального баланса скорости/качества

---

## 🆘 Если что-то не работает

1. Запустите `python3 setup.py` для диагностики
2. Проверьте логи в консоли (ищите ошибки)
3. Читайте [INSTALLATION.md](INSTALLATION.md) для деталей
4. Смотрите примеры в [examples.py](examples.py)

---

## ✅ Проверочный список

- [ ] Python 3.8+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Получены api_id и api_hash с my.telegram.org
- [ ] config.json создан и заполнен
- [ ] Первый запуск выполнен (`python3 main.py`)

---

## 🎉 Готово!

Теперь ваш Telegram бот работает с поддержкой:
- ✅ .session файлов
- ✅ Автоматических ответов через LLM
- ✅ Пересылки медиа в архив

Удачи! 🚀
