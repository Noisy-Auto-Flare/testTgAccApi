# 📚 Практический гайд: Как использовать accounts в config.json

## 🎯 Вы сейчас в файле: config.json

Этот файл - сердце всей конфигурации. Давайте разберёмся, как его правильно использовать.

---

## 📋 Текущая структура config.json

```json
{
  "telegram": {
    "api_id": 123456,
    "api_hash": "your_api_hash_here"
  },
  "accounts": [
    { "account1": {...} },
    { "account2": {...} }
  ]
}
```

### Верхний уровень (telegram)
Это **глобальные настройки**, единые для всех аккаунтов:
- `api_id` - ваш Telegram API ID (получить на my.telegram.org)
- `api_hash` - ваш Telegram API Hash

---

## 👤 Уровень "accounts" - Ваши аккаунты

Каждый элемент в массиве `accounts` - это **отдельный аккаунт** с независимыми функциями.

### Пример 1: Простейший аккаунт (только авторизация)

```json
{
  "accounts": [
    {
      "name": "mybot",
      "session_file": null,
      "phone": "+79001234567",
      "llm": {
        "enabled": false
      },
      "media_forward": {
        "enabled": false
      }
    }
  ]
}
```

**Что происходит при запуске:**
1. Программа видит аккаунт "mybot"
2. session_file = null → нужна авторизация
3. phone = "+79001234567" → используется этот номер
4. Запрашивается код из Telegram
5. После ввода кода - авторизация успешна
6. Сессия сохраняется автоматически (в файл mybot.session)
7. LLM выключен → сообщения не обрабатываются
8. Media Forwarder выключен → медиа не пересылается

**Результат:** просто авторизованный аккаунт, ловит входящие сообщения

---

### Пример 2: Аккаунт с готовой сессией (.session файл)

```json
{
  "accounts": [
    {
      "name": "backup_account",
      "session_file": "./sessions/backup.session",
      "phone": null,
      "llm": { "enabled": false },
      "media_forward": { "enabled": false }
    }
  ]
}
```

**Что происходит при запуске:**
1. session_file указан → берётся готовая сессия
2. phone = null → авторизация не требуется
3. Сессия загружается из файла
4. Если сессия валидна → готово к работе
5. Если сессия устарела → ошибка (нужна переавторизация)

**Результат:** быстрый запуск без ввода кода, нужен готовый .session файл

**Где взять .session файл:**
```bash
# Способ 1: Через интерактивный помощник
python3 setup.py
# Выбрать пункт 2 "Авторизоваться и сохранить .session"
# Файл сохранится в sessions/account_name.session

# Способ 2: После первого запуска с phone
# После первого запуска с указанным phone файл сохранится
# Потом можно переключиться на session_file
```

---

### Пример 3: Аккаунт с LLM (авто-ответы)

```json
{
  "accounts": [
    {
      "name": "smart_bot",
      "session_file": "./sessions/smart.session",
      "phone": null,
      "llm": {
        "enabled": true,
        "api_url": "http://127.0.0.1:5000/api/v1/chat/completions",
        "api_key": null,
        "system_prompt": "Ты дружелюбный помощник. Помогай людям. Отвечай кратко.",
        "allowed_chat_ids": [123456789],
        "timeout": 60
      },
      "media_forward": {
        "enabled": false
      }
    }
  ]
}
```

**Что происходит при запуске:**
1. Аккаунт загружается
2. LLM включен → начинает слушать входящие сообщения
3. **allowed_chat_ids** = [123456789] → обрабатывает только сообщения из этого чата
4. Каждое входящее сообщение:
   - Отправляется на LLM API
   - LLM генерирует ответ
   - Ответ отправляется обратно в чат
5. Перед отправкой на LLM отправляется "печатает..." (typing action)

**Параметры LLM:**

| Параметр | Пример | Описание |
|----------|--------|---------|
| enabled | true | Включить/выключить обработку |
| api_url | http://127.0.0.1:5000/api/v1/chat/completions | Адрес вашей LLM API |
| api_key | null | API ключ (если нужен) |
| system_prompt | "Ты помощник" | Роль и инструкции для AI |
| allowed_chat_ids | [123456789] | Какие чаты обрабатывать |
| timeout | 60 | Макс. время ожидания ответа |

**Как узнать allowed_chat_ids:**
```bash
python3 setup.py
# Выбрать пункт 3 "Получить IDs чатов и каналов"
# Результаты сохранятся в chat_ids.json
```

**Как проверить API:**
```bash
python3 setup.py
# Выбрать пункт 4 "Проверить соединение с LLM API"
```

---

### Пример 4: Аккаунт с Media Forwarder (архивирование медиа)

```json
{
  "accounts": [
    {
      "name": "archiver",
      "session_file": "./sessions/archiver.session",
      "phone": null,
      "llm": {
        "enabled": false
      },
      "media_forward": {
        "enabled": true,
        "source_chat_ids": [123456789, -100987654321],
        "target_channel_id": -100555666777,
        "include_captions": true
      }
    }
  ]
}
```

**Что происходит при запуске:**
1. Аккаунт загружается
2. Media Forwarder включен → мониторит определённые чаты
3. **source_chat_ids** - какие чаты слушать:
   - `123456789` = личный чат или группа (положительное число)
   - `-100987654321` = канал (отрицательное число, начинается с -100)
4. Когда приходит фото или видео:
   - Перехватывается сообщение
   - Пересылается в **target_channel_id**
   - Сохраняется оригинальная подпись
   - Если include_captions = true, добавляется информация об источнике

**Параметры Media Forward:**

| Параметр | Пример | Описание |
|----------|--------|---------|
| enabled | true | Включить/выключить пересылку |
| source_chat_ids | [123456789, -100987654321] | Откуда брать медиа |
| target_channel_id | -100555666777 | Куда пересылать |
| include_captions | true | Добавлять ли информацию об источнике |

**Типы ID чатов:**
- Личный диалог: положительное число (например, `123456789`)
- Группа: положительное число (например, `987654321`)
- Канал: отрицательное число с -100 в начале (например, `-100123456789`)

**Как узнать нужные IDs:**
```bash
python3 setup.py
# Выбрать пункт 3 "Получить IDs чатов и каналов"
```

---

### Пример 5: ВСЕ ФУНКЦИИ ВМЕСТЕ в одном аккаунте

```json
{
  "accounts": [
    {
      "name": "super_bot",
      "session_file": "./sessions/super.session",
      "phone": null,
      "llm": {
        "enabled": true,
        "api_url": "http://127.0.0.1:5000/api/v1/chat/completions",
        "api_key": null,
        "system_prompt": "Ты смешной бот-помощник!",
        "allowed_chat_ids": [111111111],
        "timeout": 60
      },
      "media_forward": {
        "enabled": true,
        "source_chat_ids": [111111111, -100222222222],
        "target_channel_id": -100333333333,
        "include_captions": true
      }
    }
  ]
}
```

**Что происходит:**
1. ✅ LLM включена → отвечает на сообщения в чате 111111111
2. ✅ Media Forwarder включена → архивирует медиа из двух источников
3. **Работают параллельно и независимо!**

---

## 👥 НЕСКОЛЬКО АККАУНТОВ

Можно управлять несколькими аккаунтами из одного config.json:

```json
{
  "telegram": {
    "api_id": 123456,
    "api_hash": "your_api_hash_here"
  },
  "accounts": [
    {
      "name": "account1_smart",
      "session_file": "./sessions/account1.session",
      "phone": null,
      "llm": {
        "enabled": true,
        "api_url": "http://127.0.0.1:5000/api/v1/chat/completions",
        "system_prompt": "Ты помощник",
        "allowed_chat_ids": [111111],
        "timeout": 60
      },
      "media_forward": {
        "enabled": false
      }
    },
    {
      "name": "account2_archiver",
      "session_file": "./sessions/account2.session",
      "phone": null,
      "llm": {
        "enabled": false
      },
      "media_forward": {
        "enabled": true,
        "source_chat_ids": [222222],
        "target_channel_id": -100999999,
        "include_captions": true
      }
    },
    {
      "name": "account3_monitor",
      "session_file": "./sessions/account3.session",
      "phone": null,
      "llm": {
        "enabled": false
      },
      "media_forward": {
        "enabled": false
      }
    }
  ]
}
```

**Запуск:**
```bash
python3 main.py
```

**Результат:**
- Account 1: отвечает на сообщения в чате 111111
- Account 2: архивирует медиа из чата 222222
- Account 3: просто мониторит (никакие функции не включены)
- **ВСЕ РАБОТАЮТ ОДНОВРЕМЕННО!**

---

## 🔄 ШАГ ЗА ШАГОМ: От нуля до рабочего бота

### Этап 1: Получить API credentials

```bash
# 1. Откройте https://my.telegram.org
# 2. Войдите по номеру телефона
# 3. Перейдите в "API development tools"
# 4. Создайте новое приложение:
#    App title: testTgAccApi
#    Short name: testacc
# 5. Скопируйте api_id и api_hash
```

### Этап 2: Отредактировать config.json

```bash
# 1. Откройте config.json
nano config.json

# 2. Замените YOUR_API_ID на полученный api_id (число)
# 3. Замените YOUR_API_HASH на полученный api_hash (строка)
# 4. Замените +79001234567 на ваш номер телефона

# Сохраните (Ctrl+O, Enter, Ctrl+X в nano)
```

### Этап 3: Первый запуск (авторизация)

```bash
python3 main.py

# При первом запуске:
# 1. Введите номер телефона: +79001234567
# 2. Откройте Telegram и найдите сообщение с кодом
# 3. Введите код в консоль
# 4. Если 2FA включена, введите пароль
# 5. Готово!
```

Сессия автоматически сохранится в `mybot.session`

### Этап 4: Переключиться на .session файл (опционально)

```json
{
  "session_file": "./sessions/mybot.session",
  "phone": null
}
```

Теперь при каждом запуске авторизация будет автоматической!

### Этап 5: Добавить LLM (если нужна)

**Шаг 1:** Установите text-generation-webui
```bash
git clone https://github.com/oobabooga/text-generation-webui.git
cd text-generation-webui
bash start_linux.sh  # или start_macos.sh
```

**Шаг 2:** Откройте http://127.0.0.1:5000 и:
- Session → Server
- Включите "OpenAI-like API"
- Сохраните

**Шаг 3:** В config.json:
```json
{
  "llm": {
    "enabled": true,
    "api_url": "http://127.0.0.1:5000/api/v1/chat/completions",
    "system_prompt": "Ты помощник",
    "allowed_chat_ids": [123456789],
    "timeout": 60
  }
}
```

**Шаг 4:** Узнайте ID чата:
```bash
python3 setup.py
# Выбрать пункт 3
```

**Шаг 5:** Запустите:
```bash
python3 main.py
```

### Этап 6: Добавить Media Forwarder (если нужна)

**Шаг 1:** Создайте приватный канал в Telegram (@myarchive)

**Шаг 2:** Узнайте ID чатов-источников и канала:
```bash
python3 setup.py
# Выбрать пункт 3 "Получить IDs чатов и каналов"
```

**Шаг 3:** В config.json:
```json
{
  "media_forward": {
    "enabled": true,
    "source_chat_ids": [111111, -100222222],
    "target_channel_id": -100333333,
    "include_captions": true
  }
}
```

**Шаг 4:** Запустите:
```bash
python3 main.py
```

---

## 🔍 ОТЛАДКА И ПРОВЕРКА

### Проверить синтаксис config.json

```bash
python3 -c "import json; json.load(open('config.json')); print('✓ Valid JSON')"
```

### Получить IDs чатов

```bash
python3 setup.py
# Выбрать пункт 3
# Результат в chat_ids.json
```

### Проверить LLM API

```bash
python3 setup.py
# Выбрать пункт 4
```

### Запустить примеры

```bash
python3 examples.py
# Выбрать нужный пример (1-5)
```

### Запустить с логами

```bash
python3 main.py 2>&1 | tee log.txt
# cat log.txt  # посмотреть логи
```

---

## ✅ ПРОВЕРОЧНЫЙ СПИСОК

Перед запуском убедитесь:

- [ ] `api_id` - число (не строка)
- [ ] `api_hash` - строка 32+ символа
- [ ] `name` - уникальное для каждого аккаунта
- [ ] `session_file` ИЛИ `phone` - один из них обязателен
- [ ] Если используется `session_file` - файл существует
- [ ] Если используется `phone` - формат +7XXXXXXXXXX
- [ ] `allowed_chat_ids` - это массив чисел (может быть пустым)
- [ ] `target_channel_id` - для media_forward это отрицательное число
- [ ] JSON синтаксис правильный (проверьте скобки и запятые)

---

## 🎉 ВСЁ ГОТОВО!

Теперь вы знаете как:
- ✅ Структурировать config.json
- ✅ Добавлять новые аккаунты
- ✅ Включать/выключать функции
- ✅ Находить нужные IDs
- ✅ Настраивать LLM
- ✅ Настраивать Media Forwarder

**Запустите:** `python3 main.py`
